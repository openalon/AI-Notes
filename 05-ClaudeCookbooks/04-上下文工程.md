---
id: n_13f9a4e28ae4
---

# 第 4 章：上下文工程

<details class="note-cite">
<summary>出处</summary>
<p>来源：<a href="https://github.com/anthropics/claude-cookbooks/blob/main/tool_use/context_engineering/context_engineering_tools.ipynb">context_engineering_tools.ipynb</a> · 状态：已读</p>
</details>

## 一句话

**上下文是预算，不是垃圾桶。** cache 决定重复前缀的单价，compaction / memory / tool-clearing 决定长任务还能不能活过第 N 轮，extended thinking 是另一笔要记账的注意力。

## 核心概念

### Prompt cache：自动先于精细

`misc/prompt_caching.ipynb` 两种开法：

| | 自动 | 显式 breakpoint |
|---|---|---|
| 用法 | 请求顶层一个 `cache_control` | 打在具体 content block 上 |
| 多轮 | breakpoint 自己往前挪 | 你自己挪 |
| 控制 | 一个 TTL | 最多 4 个独立点，可混 TTL |

建议从自动开始。细节（当时文档）：Sonnet 最少 1024 token 才缓存；Opus / Haiku 4.5 要 4096。默认 TTL 5 分钟，命中刷新；1 小时 TTL 写价 2x。**写 1.25x，读 0.1x。** 自动占用 4 个名额里的一个。

`misc/speculative_prompt_caching.ipynb`：用户还在打字时发 1-token 预热，把 cache write 藏进等待。大上下文（>1k）反复查询最划算。预热和正式请求必须字节级同前缀；加 timestamp 防串会话。

### Batch：不在线，换半价

`misc/batch_processing.ipynb`：Message Batches 异步，**−50% 费用**。适合离线评测、批量分类、隔夜摘要。同一 batch 可混 system、多轮、带图。交互产品不走这条。

### Compaction：线性历史会先杀死客服 agent

`tool_use/automatic-context-compaction.ipynb`：5 张工单 × 7 步 ≈ 35 次工具，第 27 轮累计约 15 万 input，只为 5 张票。工单 A 的分类和草稿在处理 B 时仍占窗口。SDK `compaction_control`：超阈值注入摘要 user turn，模型打 `<summary>`，清历史，只留摘要继续。Opus 4.6 起优先 **服务端 compaction**；SDK 方案留给旧模型或想用更便宜模型做摘要的时候。

`misc/session_memory_compaction.ipynb` 针对对话产品：传统 compaction 撞硬顶再摘要，示例里用户干等 **40+ 秒**。Instant compaction 在软阈值后台线程写 `session-memory.md`，硬顶时直接换入。后台摘要把整段对话标 cache，增量指令才按全价，更新可到 ~10x 便宜 / ~80% 降本。和 SDK 自动压历史是两条线：一个面向 agent 工具环，一个面向聊天连续性。

### Memory 是工具，不是更长的 system

`tool_use/memory_cookbook.ipynb` 对齐 Anthropic《Effective context engineering》：跨会话要持久，长会话要腾窗口。memory tool + context editing：该记住的写成文件，该忘的清掉。`context_engineering_tools.ipynb` 把 **memory / compact / tool-clearing** 放在一张桌上比——组合，不是三选一。Book 1 [[05-上下文治理]] 是同一制度在 Claude Code 里的实现。

### Extended thinking 也占窗口

`extended_thinking/extended_thinking.ipynb`、`extended_thinking_with_tool_use.ipynb`：思考块要回传才能续想，**思考本身是上下文开销**。和工具叠用时，adaptive thinking（结果意外才想）比每步都想便宜。第 10 章复现官方深搜分数时，`thinking: adaptive`、effort、budget、compaction 是一组旋钮，不是四个装饰。

对照 recipe：

| 路径 | 钉的是什么 |
|---|---|
| `misc/prompt_caching.ipynb` | 前缀可复用才值得 cache |
| `misc/speculative_prompt_caching.ipynb` | 用户打字时预热缓存 |
| `misc/batch_processing.ipynb` | Message Batches：不在线、换吞吐 |
| `tool_use/automatic-context-compaction.ipynb` | 长代理自动压历史 |
| `misc/session_memory_compaction.ipynb` | 对话产品：后台预写记忆，硬顶瞬间换入 |
| `tool_use/memory_cookbook.ipynb` | memory 作为工具，而不是「全塞 system」 |
| `tool_use/context_engineering/context_engineering_tools.ipynb` | memory / compact / tool clearing 对照与组合 |
| `extended_thinking/extended_thinking.ipynb` | 思考块是另一笔上下文开销 |
| `extended_thinking/extended_thinking_with_tool_use.ipynb` | 边想边调工具 |

## 关键洞察

上下文工程的决策树其实很短：

1. 前缀稳、多轮重复 → cache（必要时投机预热）
2. 不需要人等 → batch
3. 工具结果线性堆积 → compact 或 PTC（第 3 章）或清工具
4. 跨会话的偏好 / 事实 → memory 文件，不要指望窗口
5. 思考深度 → 单独记账，不要和「更长 prompt」混在一起买

「把什么都贴进 system」是最贵的默认。Book 1 用 section 边界保护缓存；这里用 API 原语做同一件事。

## 个人思考

第 2 章 contextual retrieval 已经依赖 cache 才跑得起；第 3 章 PTC 从源头少灌结果。本章是收口：还是灌进来了，怎么活。下一章工作流会把单次调用乘成 N+1——没有预算制度，orchestrator 只是加速破产。

## 相关

- [[00-Cookbooks阅读地图]]
- [[05-上下文治理]]
- [[03-工具调用]]
- [[10-评测与成本]]
